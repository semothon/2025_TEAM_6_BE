spring:
  application:
    name: KHU_semothon
  datasource:
    url: jdbc:mysql://localhost:3306/semothon?useSSL=false&serverTimezone=Asia/Seoul
    username: root
    password: 7897
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # 개발 중에는 update, 배포 시에는 validate나 none으로 변경
      show-sql: true # SQL 쿼리 로그 확인하려면 true

